---
export interface Props {
  linkname: string;
  alt: string;
}

import { Image } from "@astrojs/image/components";

const { linkname, alt } = Astro.props;
---

<Image src={import(linkname)} alt={alt} />
